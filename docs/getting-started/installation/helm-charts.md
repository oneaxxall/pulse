# Helm Charts

Socket is also available as a Helm Chart for deployment in Kubernetes environments. The installation details and the chart are available in a separate repository on GitHub ([charts/Socket](https://github.com/Socket/charts/tree/master/charts/Socket)). You will find complete installation instructions to configure and run the application in any Kubernetes cluster within this repository.
